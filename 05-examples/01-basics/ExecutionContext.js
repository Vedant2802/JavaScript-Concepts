var n = 2;
function square(num) {
  ans = num * num;
  return ans;
}

// num is the parameter of the function and n is the argument that we are passing

var square2 = square(n);
var square3 = square(4);
console.log("first", square3);
